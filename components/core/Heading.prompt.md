Every headline in the brand is this serif — never the sans.

```jsx
<Heading level={1} size="xl" ground="ink">
  We Build <HeadingAccent>Timeless</HeadingAccent> Spaces.
</Heading>
```

Headlines break across 2–3 short lines; keep line length under ~26 characters at xl.
