Hairline separator — the only divider treatment in the brand (no thick rules, no shadow separators).

```jsx
<Divider ground="ink" />
<Divider orientation="vertical" length="46px" ground="ink" />
```
