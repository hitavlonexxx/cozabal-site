Wraps every photograph. Use `overlay` whenever a caption sits on top of the image.

```jsx
<ImageFrame src="/assets/img/horizon.jpg" ratio="3 / 4" overlay />
<ImageFrame label="Interior — dusk" ratio="4 / 3" />
```

No supplied photography ships with this system; omit `src` to show the placeholder plate.
