/** Circular hairline social links. Lucide brand glyph names. */
export interface SocialRowProps { networks?: string[]; size?: number }
export declare function SocialRow(props: SocialRowProps): JSX.Element;
