/** Rounded, blurred ink capsule holding StatItem children separated by vertical hairlines. */
export interface StatBarProps { children?: React.ReactNode; style?: React.CSSProperties }
export declare function StatBar(props: StatBarProps): JSX.Element;
